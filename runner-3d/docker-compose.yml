services:
  api:
    build: ./backend
    ports: ["8003:8003"]
    env_file: ./backend/.env
    extra_hosts:
      - "host.docker.internal:host-gateway"
  web:
    build: ./frontend
    ports: ["5195:80"]
    depends_on: [api]
