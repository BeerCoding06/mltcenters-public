# Backend (server/) – copy to `.env` in project root or `server/.env`
OPENAI_API_KEY=
PORT=3000

# Registration email → mltcenterth@gmail.com
# Gmail: enable 2-Step Verification, then create App Password at
# https://myaccount.google.com/apppasswords
REGISTER_TO_EMAIL=mltcenterth@gmail.com
SMTP_USER=mltcenterth@gmail.com
SMTP_PASS=
# Optional overrides (defaults work for Gmail)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
