# Backend (server/) – OpenAI API key. Never commit the real key.
# Copy to server/.env and set OPENAI_API_KEY=sk-...
OPENAI_API_KEY=
PORT=3000

# Frontend (Vite) – optional; API is proxied to backend in development.
# VITE_API_URL=http://localhost:3000
